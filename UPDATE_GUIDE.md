# 리뉴얼 v0 — 콘텐츠 계승 (검토용)

이번 패키지는 **배포용이 아니라 검토용**입니다.
기존 `content.js`의 내용을 새 구조(`data.js`)로 마이그레이션했고,
그 데이터를 렌더링하는 **텍스트 모드(text.html)**를 먼저 완성했습니다.
게임 모드(index.html)는 다음 버전(v1: 아이소메트릭 코어)부터 들어갑니다.

## 확인 방법

압축을 풀고 `text.html`을 브라우저로 열면 됩니다. (더블클릭 OK, 배포 불필요)

```bash
cd ~/Downloads
unzip -o profile_v0.1_content.zip -d profile-v2-preview
open profile-v2-preview/text.html
```

> 기존 `~/Developer/profile`은 아직 건드리지 마세요.
> 새 사이트가 게임 모드까지 완성되면 그때 한 번에 교체하는 걸 추천합니다.

## 파일 구조

```
newsite/
├── text.html            ← 텍스트 모드 (탭형 문서)
├── assets/js/data.js    ← 단일 콘텐츠 소스 (게임/텍스트 공용) ★내용 수정은 여기
└── videos/              ← 기존 영상 2개 계승
    ├── questions_program.mp4   (문항 코드 자동화 시연)
    └── jingum_test.mp4         (답파고 시연)
```

## 계승 vs 새로 쓴 것

| 구분 | 상태 |
|---|---|
| 학력 / 교육 수료 / 경력(천재·EBS) / 자격증 / 언어 / 수상 | ✅ 기존 내용 그대로 계승 |
| 문항 코드 자동화, 답파고 프로젝트 | ✅ 계승 (Projects 탭으로 재배치) |
| About (올라운더 소개, 역량 스탯, 업적 배지) | 🔴 새로 쓴 초안 — 검토 필요 |
| RPG 포트폴리오 사이트 (프로젝트 항목) | 🔴 새로 쓴 초안 — 검토 필요 |
| 스킬 목록 | 🔴 기존 내용에서 추출·재구성한 초안 — 검토 필요 |

페이지 안에서 빨간 **[초안 — 검토 필요]** 배지가 붙은 부분이 새로 쓴 곳입니다.

## 검토 포인트

1. About의 "올라운더" 소개 문장 — 톤이 지영님 목소리와 맞는지
2. 역량 스탯 수치(1~5) — 셀프 평가로 조정
3. 업적 배지 6개 — 빼거나 바꾸고 싶은 것
4. 스킬 3개 그룹 분류 — 누락/과장된 항목
5. "단독 개발 (기여 100%)" 같은 기여도 표기 — 그대로 둘지

## v0.1 변경 사항 (2026.07.02)

- 문항 코드 프로젝트를 최신 버전 기준으로 재작성: 「초중고 PDF 문항 코드 추출기」
  - GUI 프로그램(.exe) 도구화 + 사용 가이드 제작·배포 서사 추가
  - 구버전 시연 영상(questions_program.mp4) 제거
  - 대표 이미지: 사용 가이드 속 메인 화면 (images/item_extractor_main.png)
  - 새 시연 영상 촬영 후 data.js의 media를 video로 교체하면 됨
- 기여도 % 표기 제거 ("단독 개발"만 유지)

## v1 변경 사항 (2026.07.03)

### 콘텐츠
- 경력 기간을 일자 단위로 반영: 천재교과서 2021.08.02~2023.06.23 (1년 10개월) / EBS 2024.07.18~2026.07.17 (2년)
- Career 탭 상단에 **총 경력 3년 10개월** 자동 계산 표시
  - data.js의 period 날짜만 고치면 총 경력·회사별 기간이 자동으로 다시 계산됨

### 게임 모드 (아이소메트릭 코어) 🆕
- `index.html` — 게임 진입점. 우측 상단 "📄 문서로 보기" ↔ text.html 상호 연결
- `assets/js/game.js` — 코어 엔진
  - 마름모 타일 그리드 렌더링 (12×12, 잔디 2종 + 길)
  - 타일 단위 캐릭터 이동: 방향키/WASD, 모바일은 화면 D-패드
  - 깊이 정렬: 캐릭터가 나무·건물 뒤로 가면 가려지고 앞에서는 가림
  - 충돌 처리: 오브젝트 점유 타일 진입 불가
- `tools/generate_assets.py` — PIL 에셋 생성기 (색·파라미터 수정 후 재실행하면 에셋 갱신)
- `images/game/` — 생성된 에셋 (타일, 나무, 바위, 건물 틀, 캐릭터 시트)

### 확인 방법
로컬에서 `index.html`을 열면 바로 플레이 가능. 확인 포인트:
1. 캐릭터 이동이 부드러운지 (한 칸씩, 200ms)
2. 나무/건물 뒤로 갔을 때 자연스럽게 가려지는지 ← v1의 핵심
3. 모바일에서 D-패드 조작감
4. 건물은 아직 자리 표시용 — 상호작용/실제 건물 7종은 v2에서

### 다음 (v2 예정)
- 건물 7종 배치 (지영의 집 / 학교 / 천재교과서 / EBS / 작업실 / 성장 기록관 / 우체국)
- 건물 앞 상호작용(입장/대화) + data.js 콘텐츠 연결

## v2 변경 사항 (2026.07.03)

### 마을 완성 — 건물 7종 + 상호작용 🆕
- 맵 16×16 확장, 길 네트워크 자동 생성 (세로/가로 큰길 + 건물별 진입로)
- 건물 7종 배치 (전부 PIL 생성, 각기 다른 색·지붕·장식):
  | 건물 | 장식 | 연결 콘텐츠 |
  |---|---|---|
  | 🏠 지영의 집 | 굴뚝 | About (스탯 + 업적 + 소개) |
  | 🏫 천재교과서 | 벽 간판 띠 | 경력 ① |
  | 📺 EBS | 안테나 | 경력 ② |
  | ⚒️ 작업실 | — | 프로젝트 3종 (이미지/영상 포함) |
  | 📚 성장 기록관 | 지붕 위 책 더미 | 스킬·자격증·언어·수상 |
  | 🎓 학교 | 깃발 | 학력 + 교육 수료 |
  | 📮 우체국 | 빨간 우체통 | Contact |
- 건물 위 이름표 표시, 문 앞 도착 시 말풍선 힌트
- **입장**: 문 앞에서 Space/Enter (모바일: D-패드 가운데 A버튼)
- 콘텐츠 패널: 양피지 스타일 카드, ESC/✕/바깥 클릭으로 닫기
- 패널 하단 "📄 문서 모드에서 자세히 보기" → text.html 해당 탭으로 딥링크
- 검증: 시작점에서 7개 건물 문까지 전부 도달 가능 (BFS), 고립 타일 0

### 확인 포인트
1. 각 건물 입장 → 콘텐츠가 맞게 뜨는지 (특히 작업실의 이미지/영상)
2. 패널 열림/닫힘 중 캐릭터가 움직이지 않는지
3. 모바일에서 A버튼이 문 앞에서만 초록색으로 활성화되는지
4. 마을 배치·건물 색감 취향 (generate_assets.py에서 조정 가능)

### 다음 (v3 후보)
- 배포 준비: 저장소 교체 커밋 가이드
- 폴리싱: 걸을 때 발소리/BGM 토글, 건물 입장 트랜지션, 시작 타이틀 화면

## v2.1 변경 사항 (2026.07.03)

### ① 콘텐츠 → NPC 대화 형식 전환
- 문서형 패널 제거 → **하단 RPG 대화창**으로 교체
  - 화자: 지영 NPC (픽셀 초상화 + 이름표 + 현재 건물 표시)
  - 타자 효과로 한 줄씩 출력 (Space/Enter/탭 → 줄 넘김, 타이핑 중이면 즉시 완성)
  - 선택지 분기: 건물마다 메뉴 대화 (예: 작업실 — "어떤 작업물이 궁금하세요?")
  - 스탯 바·업적 배지·프로젝트 미디어는 대화 중 **대화창 위 카드**로 등장
  - 모든 건물 대화에 "📄 문서로 자세히 보기" 선택지 (text.html 해당 탭 딥링크)
  - ESC / ✕ 로 대화 종료
- 대화 톤: 지영이 방문자에게 직접 안내하는 해요체

### ② 문 위치 수정
- 건물 스프라이트의 문이 오른쪽 벽(동쪽 면)에 있는데 입장 타일이 남쪽에 있던 문제 해결
- **모든 입장 타일을 문 앞(동쪽)으로 이동** + 길을 문 앞까지 재배치
- 성장 기록관 위치 조정 (11,5) — 길과 발자국 충돌 제거
- 검증: 7개 건물 모두 문-동쪽 인접 ✅ / 문 앞까지 길 연결 ✅ / BFS 전체 도달 ✅

### 확인 포인트
1. 각 건물 문(오른쪽 벽 갈색 문) 앞에서 입장되는지
2. 대화 넘김 조작감 (타자 속도, 탭/Space 반응)
3. 선택지 메뉴 흐름 (돌아오기/그만 보기)
4. 카드(스탯/배지/프로젝트 이미지·영상)가 대화와 함께 잘 뜨는지

## v2.2 변경 사항 (2026.07.03)

### ① 위치 초기화 문제 해결
- 원인: "문서로 보기" 등의 링크가 **같은 탭**에서 열려서, 돌아올 때 페이지가 새로 로드되며 시작점으로 리셋
- 해결 1: 대화 속 문서 딥링크를 **새 탭**으로 변경 (게임 화면 유지)
- 해결 2: **위치·방문 기록·좋아요를 localStorage에 저장** — 새로고침하거나 나갔다 와도 서 있던 자리에서 이어짐
  - 저장 키: `jy_village_v1` (초기화하고 싶으면 브라우저 개발자도구에서 해당 키 삭제)

### ② 탐험 진행도 + GAME CLEAR
- 우측 상단 진행도 칩: 🗺️ 3/7 (클리어 후엔 👑)
- 방문한 건물은 이름표에 **✓ 표시** + 초록 텍스트 → 어디를 봤는지 한눈에
- 7곳을 모두 방문하고 대화를 닫는 순간 **🎉 GAME CLEAR 카드** 등장
  (좋아요 남기기 / 문서로 보기 / 계속 둘러보기)

### ③ 💚 좋아요 기능
- 우측 상단 하트 버튼 + 우체국 대화 선택지("좋아요 남기기") + 클리어 화면
- 누르면 하트가 팡팡 터지고, 우체국에서 누르면 지영 NPC가 고마워하는 대사
- ⚠️ 현재는 **방문자 각자의 브라우저에만 저장**되는 카운트 (정적 사이트 한계)
  - 전체 방문자 합산 카운트가 필요하면 무료 카운터 서비스/간단한 백엔드 연동 필요 — 원하면 다음 버전에서

### 확인 포인트
1. 문서 갔다가 돌아와도 / 새로고침해도 위치 유지되는지
2. 7곳 방문 후 GAME CLEAR 뜨는지 (이미 방문 기록이 있다면 남은 곳만 돌면 됨)
3. 좋아요 하트 애니메이션 & 우체국 대사

## v2.3 변경 사항 (2026.07.03)

### ① 폰트 전반 축소
- 게임 UI: 대사 15→13.5px, 대화창 카드 13.5→12.5px, HUD/버튼 한 단계씩 축소, 건물 이름표·말풍선 축소
- 문서 모드(text.html): 제목 34→29px, 본문 15→14px 등 전 항목 한 단계씩 축소

### ② 대화창 높이 고정 + 선택지 분리
- 대화창(.dlg-box) 높이 112px 고정 — 대사 줄 수와 무관하게 흔들리지 않음
  (긴 대사는 대화창 안에서 스크롤)
- 선택지 버튼을 대화창 **바로 위**로 분리 (우측 정렬, 없을 땐 완전히 숨김)
- 화면 구성: [카드] → [선택지] → [대화창(고정)] 순서로 위에서 아래

## v2.4 변경 사항 (2026.07.03)

### ① 대화창 스크롤 제거
- 긴 대사를 문장 단위로 자동 분할해 여러 번에 나눠 말하도록 변경 (78자 제한)
- 검증: 전체 대사 22건 → 41개 청크, 모두 3줄 이내 / 분할 후 내용 손실 0
- 대사 첫 조각에만 카드가 붙고, 이어지는 조각에서도 카드는 유지됨

### ② 선택지 전환 시 화면 흔들림 제거
- 원인: 선택지가 나타났다 사라질 때 + 메뉴마다 버튼 줄 수가 달라 높이가 출렁임
- 해결: 선택지 영역을 **항상 1줄 고정 높이(42px)**로 예약
  - 선택지가 없어도 공간 유지 → 대화 중에도 레이아웃 불변
  - 버튼이 많으면 좌우 스와이프/스크롤 (스크롤바 숨김)

## v2.5 변경 사항 (2026.07.03)

### 대사 전면 재작성 — 설명 톤으로
- 문제: 일부 대사가 data.js의 문서 문장("~했습니다")이나 항목 나열을 그대로 낭독 → 카드와 중복
- 역할 분리: **대사 = 맥락과 이야기를 풀어 설명 (해요체)** / **카드 = 항목·수치 정리**
- 재직 기간은 자연어로 자동 변환: "2021년 8월부터 2023년 6월까지, 1년 10개월 동안 일했어요."
  (data.js의 period 날짜를 고치면 대사도 자동으로 바뀜)
- 대사 원문 위치: **data.js 맨 아래 `dialogue` 섹션** — 대사 수정도 data.js 한 파일에서 (단일 소스 유지)
- 검증: 전체 79줄 — 문서체 잔존 0건, 분할 후 모든 줄 3줄 이내, 프로젝트 대사 매핑 완비

## v2.6 변경 사항 (2026.07.03)

### EBS 경력에 카카오톡 채널 업무 추가
- 업무 항목 추가: "카카오톡 채널 구축·운영 — 채널 개설과 초기 설정, 메뉴 구성, 관리자 권한 관리 등
  고객 접점 채널의 기반 세팅 전반 담당"
- 게임 대사에도 반영 + 올라운더 서사로 마무리하는 줄 추가
  ("운영이든 데이터든 채널이든 — 필요하면 일단 맡아서 굴러가게 만드는 게 제 역할")
- 문서 모드(Career 탭)에는 data.js에서 자동 반영됨

## v2.7 변경 사항 (2026.07.03)

### 알림톡 전환 성과 스토리로 통합
- 카톡 채널 항목을 "알림 채널 전환 (SMS → 카카오 알림톡)"으로 격상
  - 채널 구축(개설·초기 설정·메뉴·관리자 권한)은 기반 작업으로 포함
  - 성과: 발송 비용 절감 (구체 수치가 있으면 desc에 추가 권장 — 예: "연간 발송 비용 N% 절감")
- 게임 대사도 프로젝트 서사로 재구성 (문제 → 기반 구축 → 비용 절감)

## v2.8 변경 사항 (2026.07.03) — 최종 이력 PDF 대조 반영

- **자격증**: Azure AI Fundamentals (2023.12.06, Microsoft) 추가 — 기존 사이트에 누락되어 있던 항목
- **날짜 수정**: ADsP 취득일 2025.09.05 → **2025.09.20** (PDF 기준)
- **수상명**: "특별상" → 공식 명칭 **"알파고 특별상"** (수상 목록 + 답파고 태그)
- **문항코드 성과**: "1시간+ → 20분" → PDF 기준 **"처리 시간 약 1/6로 단축"** + ChatGPT 활용 개발 명시
- **EBS**: 소속 "디지털교육서비스부" 표기, 데이터 업무를 **"530만 건 이상 학습 로그 정리·정규화 + 엑셀 매크로 보고 자동화"**로 수치 강화
- **천재교과서**: "디지털 콘텐츠 전환" — HTML 기반 문항 구축, 듣기·녹음·화면 기능 검수 명시
- 업적 배지 "자격증 수집가" 5종 → 6종
- 게임 대사에도 전부 동일 반영

### 새 시연 영상 (원본 수령)
- 문항코드_추출_프로그램_영상.mp4: 1920×1080 / 30fps / 약 10분 / 51MB
- 편집 후 교체 예정 — 편집은 Claude가 ffmpeg로 처리 가능 (구간 자르기, 배속, 압축 등)

## v2.9 변경 사항 (2026.07.04)

### ① 선택지 가로 스크롤 — PC 지원
- 마우스 드래그로 좌우 스크롤 가능 (드래그 시 grab 커서, 버튼 오클릭 방지)
- 마우스 휠로도 좌우 스크롤 가능

### ② EBS 데이터 업무 — 사실에 맞게 정정
- "분석했다" → "**분석 가능한 형태로 정리·정규화**했다"로 정확하게
  - 530만 건 로데이터 정리·정규화 담당, 분석은 외부 전문 업체 협업으로 명시
  - 주간 방문 수·이용 건수 보고: 엑셀 매크로로 **동일 기준 자동 집계 체계** 구축
- 게임 대사도 같은 내용으로 정정 (기반을 만드는 역할이었다는 서사)

### ③ 시연 영상 교체
- 편집본(39초) 적용: HEVC 2560×1440 70MB → **H.264 1440×810 4MB** 웹 호환 변환
- 작업실 → 문항 코드 추출기 대화에서 영상 재생 (스크린샷 대체)
- 원본 대비 화질 손실 거의 없음, 모든 브라우저 재생 가능

## v2.10 변경 사항 (2026.07.04)

### ① RPG 포트폴리오 프로젝트 — AI 협업 역량으로 재구성
- 제목: "RPG 포트폴리오 웹사이트 (본 사이트) — AI 협업 개발"
- 태그: Claude(AI) 협업 개발 명시
- 블록: 기획(본인) / AI 협업 개발(요구사항 정의·방향 결정·버전별 검증과 피드백) / 개발 방식
- 인사이트: "AI와의 협업에서 사람의 몫은 정의·검증·방향 잡기" + ChatGPT(업무 도구)와 Claude(포트폴리오)
  두 사례를 묶어 "AI를 실무 전력으로 활용"하는 패턴으로 프레이밍
- 게임 대사도 솔직한 협업 서사로 재작성

### ② 영어 성적 갱신
- TOEIC 785(2024.06, 만료) → **765 (2025.08.31, 유효)** 로 교체
- TOEIC Speaking IH 150 유지

## v3.0 변경 사항 (2026.07.04) — 마무리 🎉

### ① 사운드 (Web Audio 칩튠 — 외부 파일 없이 코드 생성, 용량 +0)
- 🚶 발걸음: 한 칸 이동할 때마다 두 음이 번갈아 나는 블립
- 🎵 BGM: 아늑한 8비트 마을 멜로디 루프 (C 펜타토닉, 낮은 볼륨)
- 🚪 건물 입장 차임, 💚 좋아요 4음 징글
- HUD의 🔊/🔇 버튼으로 켜고 끄기 — 설정은 localStorage에 저장
- 브라우저 자동재생 정책상 첫 키/터치 입력 후부터 재생됨 (정상 동작)

### ② 초안 배지 전부 제거
- data.js의 draft 플래그 제거 → 문서/게임 모두 [초안 — 검토 필요] 표시 사라짐

---

# 🚀 배포 가이드 — 기존 저장소를 새 사이트로 교체

> 사전 준비: 이 zip을 풀어 newsite 폴더 위치 확인 (예: ~/Downloads/newsite)

```bash
# 1) 로컬 저장소로 이동 + 최신화
cd ~/Developer/profile
git pull

# 2) 기존 사이트를 백업 브랜치로 보존 (언제든 되돌릴 수 있게)
git checkout -b backup-old-site
git push origin backup-old-site
git checkout main        # 기본 브랜치가 master라면 master로

# 3) 기존 파일 비우기 (.git 폴더는 유지!)
find . -maxdepth 1 ! -name '.git' ! -name '.' -exec rm -rf {} +

# 4) 새 사이트 파일 복사 (newsite 폴더 "내용물"을 저장소 루트로)
rsync -av ~/Downloads/newsite/ ./

# 5) 커밋 & 푸시
git add -A
git commit -m "v3.0 전면 리뉴얼: 2.5D 아이소메트릭 마을 + NPC 대화 포트폴리오"
git push origin main

# 6) 1~2분 후 확인
# https://young-jii.github.io/profile
```

체크리스트 (배포 후):
- [ ] 게임 로드 + BGM/발걸음 (첫 키 입력 후)
- [ ] 건물 7곳 입장 + GAME CLEAR
- [ ] 작업실 → 문항 코드 추출기 영상 재생
- [ ] 📄 문서로 보기 → 6개 탭 + 총 경력 3년 10개월
- [ ] 모바일에서 D-패드 / 대화 조작
- [ ] 되돌리고 싶으면: git checkout backup-old-site 의 내용으로 복구 가능
